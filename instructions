1 ) i used very simple frontend , after cloning my applications ,just hit the command (npm install ) in the terminal all packages will be automatically installed 


2 ) for the frontend deployment i have used Netlify free service provider 

